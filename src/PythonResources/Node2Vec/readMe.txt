read each file from Data/edge_list2_w2 folder 
each file corresponds to each edgelist
run main4.py on it from you java code because this needs to be run when you show the visualization of that edgelist
in main4.py you need to specify args.input and args.output which are the edge list and edge embeddings respectively